Find SQL mismatches. Be BRIEF. NO explanations, ONLY field/expected/got/fix.

SQL:
{SQL_QUERY}

Campaign:
{CAMPAIGN_JSON}

List ONLY failing conditions (MAX 10):
• Field: [name]
• Expected: [value]
• Got: [value]
• Fix: [action]

Each line MAX 15 words.